# Database

No database or backend exists in this repository yet — this project is
scaffolded as a frontend-only client. The data shapes the frontend expects
are defined in `src/types/*.ts` and can be used directly as the basis for a
future schema (e.g. Prisma models) when a backend is added.
